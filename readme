# Qdestiny 服务框架 - 使用手册

Qdestiny 是一个轻量级的 Python 后台服务框架，可以将 Python 脚本挂在后台运行。
支持**定时任务**、**全局热键触发**和**远程 HTTP 触发**三种运行模式，无需任何外部数据库，开箱即用。

---

## 快速开始

### 1. 安装依赖

```bash
pip install -r requirements.txt
```

依赖说明：

| 包名 | 用途 |
|------|------|
| `psutil` | 进程检测与系统信息 |
| `pynput` | 全局热键监听（热键类型服务需要） |
| `pyyaml` | 读取服务配置文件 |

### 2. 启动框架

打开一个终端，在项目根目录执行：

```bash
python manage.py run
```

框架启动后会常驻运行，等待接收命令。你会看到类似输出：

```
16:34:56 [INFO] 调度器已启动
16:34:56 [INFO] Qdestiny 框架已启动 (PID: 12345, 端口: 19527)
16:34:56 [INFO] 按 Ctrl+C 或执行 'python manage.py exit' 退出
```

### 3. 启动一个服务

打开**另一个终端**，执行：

```bash
python manage.py create test
```

这会启动 `services/test.py` 中定义的服务。框架会根据 `configs/test.yaml` 的配置决定如何运行它。

### 4. 查看运行状态

```bash
python manage.py status
```

输出示例：

```
--- 框架状态 ---
  PID:    12345
  端口:   19527
  运行:   30s
  远程:   [OFF]

--- 服务列表 (1 个) ---
  [RUN] test                 类型: scheduled    执行: 2次  启动于: 2026-04-05 16:35:06
```

---

## 命令参考

所有命令都在项目根目录下执行。框架必须先通过 `run` 启动，其他命令才能使用。

| 命令 | 说明 | 示例 |
|------|------|------|
| `run` | 启动框架主进程（需保持终端运行） | `python manage.py run` |
| `create <名称>` | 启动一个服务 | `python manage.py create test` |
| `stop <名称>` | 停止一个运行中的服务 | `python manage.py stop test` |
| `status` | 查看框架和所有服务的运行状态 | `python manage.py status` |
| `list` | 列出 services/ 目录下所有可用的服务 | `python manage.py list` |
| `remove <名称>` | 从框架中移除已注册的服务 | `python manage.py remove test` |
| `remote start` | 启动远程 HTTP 服务 | `python manage.py remote start` |
| `remote stop` | 停止远程 HTTP 服务 | `python manage.py remote stop` |
| `exit` | 退出框架（会自动停止所有服务） | `python manage.py exit` |

**stop 和 remove 的区别**：`stop` 停止服务但保留注册信息（可以查到状态），`remove` 彻底从框架中删除。

---

## 服务类型

Qdestiny 支持两种服务类型，通过 YAML 配置文件指定。此外还支持远程 HTTP 触发模式，可从外网控制服务。

### 类型一：定时任务（scheduled）

按照设定的时间规则自动运行服务。

**配置示例** (`configs/test.yaml`)：

```yaml
name: test
type: scheduled
schedule:
  delay: 5          # 首次执行延迟（秒），0 表示立即执行
  interval: 10      # 重复间隔（秒）
  repeat: 3         # 运行策略（见下方说明）
```

**repeat 运行策略**：

| 值 | 含义 |
|----|------|
| `"once"` | 执行 1 次后自动移除 |
| `3`（任意正整数） | 执行 3 次后自动移除 |
| `"forever"` | 永久循环执行，直到手动 stop |

**执行流程**：框架启动服务后，等待 `delay` 秒 → 执行第 1 次 → 等待 `interval` 秒 → 执行第 2 次 → ... → 达到 `repeat` 次数后自动停止。

### 类型二：热键触发（hotkey）

按下指定的组合键时触发运行，适合需要手动触发的操作。

**配置示例** (`configs/system_monitor.yaml`)：

```yaml
name: system_monitor
type: hotkey
hotkey:
  keys: "a+d"       # 同时按下 a 和 d 键时触发
```

**按键格式**：用 `+` 连接多个键名。支持的键名：

- 字母键：`a`, `b`, `c`, ... `z`
- 修饰键：`ctrl`, `shift`, `alt`
- 特殊键：`space`, `enter`, `tab`, `esc`

**示例组合**：`"ctrl+shift+m"`, `"a+d"`, `"alt+1"`

> 注意：热键是**全局**的，即使框架的终端窗口不在前台也能响应。

### 远程 HTTP 触发（remote）

通过 HTTP API 从外网远程控制框架中的服务。框架启动时远程服务默认**关闭**，需手动开启。

#### 开启远程服务

```bash
python manage.py remote start
```

#### 配置文件

编辑 `configs/remote.yaml` 设置端口和认证方式：

```yaml
port: 8080

auth:
  # Token 认证：请求头 Authorization: Bearer <token>
  token: "your-secret-token-here"
  # Basic Auth：请求头 Authorization: Basic base64(user:pass)
  username: "admin"
  password: "your-password-here"
```

> 安全提示：远程服务监听所有网络接口（0.0.0.0），务必配置认证信息，或使用防火墙/VPN 保护。

#### API 接口

| 方法 | 路径 | 功能 |
|------|------|------|
| GET | `/api/services` | 查看所有已注册服务的状态 |
| GET | `/api/services/available` | 列出所有可用服务（等同于 `list` 命令） |
| GET | `/api/services/<名称>` | 查看指定服务的详细状态 |
| POST | `/api/services/<名称>/start` | 远程启动一个服务 |
| POST | `/api/services/<名称>/stop` | 远程停止一个服务 |

#### 认证方式

**Token 认证**：
```bash
curl -H "Authorization: Bearer your-secret-token-here" http://<IP>:8080/api/services/available
```

**Basic Auth 认证**：
```bash
curl -u admin:your-password-here http://<IP>:8080/api/services/available
```

#### 使用示例

```bash
# 从外网启动定时邮件服务
curl -X POST -H "Authorization: Bearer your-secret-token-here" \
  http://your-server:8080/api/services/email_task/start

# 查看服务运行状态
curl -H "Authorization: Bearer your-secret-token-here" \
  http://your-server:8080/api/services

# 停止服务
curl -X POST -H "Authorization: Bearer your-secret-token-here" \
  http://your-server:8080/api/services/email_task/stop
```

#### 关闭远程服务

```bash
python manage.py remote stop
```

修改配置后需要先 `remote stop` 再 `remote start` 重新加载。

---

## 创建自己的服务

创建一个服务只需要两步：

### 第 1 步：编写服务文件

在 `services/` 目录下创建一个 Python 文件，**必须包含 `start()` 函数**：

```python
# services/my_task.py

def start():
    """这个函数会被框架调用"""
    print("我的任务正在执行！")
    # 在这里写你的业务逻辑
    # ...
```

**重要规则**：
- `start()` 函数是服务的唯一入口，框架只会调用这个函数
- `start()` 应该执行完任务后**正常返回**，不要写死循环
- 如果 `start()` 抛出异常，框架会捕获并记录错误，不会影响框架本身

### 第 2 步：编写配置文件

在 `services/` 目录下创建同名的 YAML 文件：

**定时任务配置** (`configs/my_task.yaml`)：

```yaml
name: my_task
type: scheduled
schedule:
  delay: 0            # 立即执行
  interval: 300       # 每 5 分钟执行一次
  repeat: "forever"   # 永久运行
```

**热键触发配置** (`configs/my_task.yaml`)：

```yaml
name: my_task
type: hotkey
hotkey:
  keys: "ctrl+m"      # 按下 Ctrl+M 时执行
```

### 第 3 步：启动服务

```bash
python manage.py create my_task
```

---

## 使用示例

### 示例 1：每小时清理临时文件

```python
# services/cleanup.py
import os
import glob

def start():
    temp_dir = "/tmp/myapp"
    files = glob.glob(os.path.join(temp_dir, "*.tmp"))
    for f in files:
        os.remove(f)
    print(f"已清理 {len(files)} 个临时文件")
```

```yaml
# configs/cleanup.yaml
name: cleanup
type: scheduled
schedule:
  delay: 0
  interval: 3600
  repeat: "forever"
```

### 示例 2：按键截图

```python
# services/screenshot.py
def start():
    import subprocess
    subprocess.run(["scrot", "screenshot.png"])  # Linux 截图工具
    print("截图已保存")
```

```yaml
# configs/screenshot.yaml
name: screenshot
type: hotkey
hotkey:
  keys: "ctrl+shift+s"
```

---

## 常见问题

### Q: 框架未运行时执行命令会怎样？

会提示：`框架未运行，请先执行 'python manage.py run'`

### Q: 重复启动同一个服务会怎样？

会提示：`服务 'xxx' 已在运行中`，不会重复启动。

### Q: 服务的 start() 执行报错会影响框架吗？

不会。每个服务在独立线程中运行，异常会被捕获并记录到日志，框架本身不受影响。

### Q: 定时任务还没执行完，下一次触发时间到了怎么办？

框架会自动跳过本次执行，等上一次完成后再恢复调度，避免重复执行。

### Q: 如何修改服务配置？

先 `stop` 或 `remove` 服务，修改 YAML 配置文件后重新 `create`。每次 `create` 都会重新读取配置。

### Q: 框架崩溃了怎么办？

删除项目根目录下的 `.qdestiny.lock` 文件，然后重新 `python manage.py run`。

### Q: 热键功能不生效？

确保已安装 `pynput`：`pip install pynput`。在 Linux 下可能需要 root 权限或将用户加入 `input` 组。

### Q: 远程 API 返回 "认证失败"？

请确保请求头中携带了正确的认证信息。支持两种方式：
- Token：`Authorization: Bearer <token>`（token 在 `configs/remote.yaml` 的 `auth.token` 中配置）
- Basic Auth：`Authorization: Basic base64(user:pass)`（或使用 curl 的 `-u user:pass` 参数）

### Q: `/api/services` 返回为空，但 `/api/services/available` 有数据？

`/api/services` 只显示已启动（create）的服务，`/api/services/available` 显示 services/ 目录下所有可用的服务文件。需要先通过 POST 请求启动服务，才能在 `/api/services` 中看到。

### Q: 远程服务端口被占用？

修改 `configs/remote.yaml` 中的 `port` 字段为其他可用端口，然后重新 `remote start`。

---

## 日志

框架运行日志保存在 `logs/qdestiny.log`，同时会输出到启动框架的终端。
日志文件自动轮转，单个文件最大 5MB，保留 3 个备份。
