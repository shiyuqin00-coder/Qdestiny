# 列出所有服务
python manage.py list

# 启动服务
python manage.py start --service MyExampleService

# 启动另一个服务
python manage.py start --service AnotherService

# 停止服务
python manage.py stop --service MyExampleService

# 再次列出查看状态
python manage.py list