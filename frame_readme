# Qdestiny 框架实现与架构文档

本文档面向开发者，详细说明 Qdestiny 框架的内部架构、模块职责、核心流程和设计决策。

---

## 1. 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                         manage.py (CLI 入口)                     │
│                     argparse 解析 → run / 其他命令                │
├───────────────────────────┬─────────────────────────────────────┤
│      run 命令 → 主进程     │     其他命令 → 客户端进程             │
│                           │                                     │
│  ┌─────────────────────┐  │  ┌──────────────────────────────┐   │
│  │   FrameworkServer   │  │  │     FrameworkClient          │   │
│  │   (core/server.py)  │◄─┼──│     (core/client.py)         │   │
│  │                     │  │  │                              │   │
│  │  ┌───────────────┐  │  │  │  读取 .qdestiny.lock         │   │
│  │  │  TCP Server   │  │  │  │  获取端口 → TCP 连接          │   │
│  │  │  localhost:    │  │  │  │  发送 JSON 命令              │   │
│  │  │  19527        │  │  │  │  接收响应 → 格式化打印         │   │
│  │  └───────┬───────┘  │  │  └──────────────────────────────┘   │
│  │          │          │  │                                     │
│  │  ┌───────▼───────┐  │  │                                     │
│  │  │  ServiceRunner │  │  │                                     │
│  │  │ (service_     │  │  │                                     │
│  │  │  runner.py)   │  │  │                                     │
│  │  └──┬─────────┬──┘  │  │                                     │
│  │     │         │      │  │                                     │
│  │  ┌──▼──┐  ┌──▼───┐  │  │                                     │
│  │  │Sched│  │Hotkey│  │  │                                     │
│  │  │uler │  │Mgr   │  │  │                                     │
│  │  └─────┘  └──────┘  │  │                                     │
│  └─────────────────────┘  │                                     │
└───────────────────────────┴─────────────────────────────────────┘
```

**核心设计原则**：
- **零外部依赖**：不依赖 Redis、数据库等外部服务，仅使用 Python 标准库 + 少量第三方包
- **进程分离**：框架主进程常驻运行，CLI 命令作为独立进程通过 TCP 通信
- **线程隔离**：每个服务的 `start()` 在独立线程中执行，异常不影响框架
- **跨平台**：所有模块兼容 Windows / Linux / macOS

---

## 2. 项目结构

```
Qdestiny/
├── manage.py                    # CLI 入口，唯一的用户交互点
├── requirements.txt             # 项目依赖
├── .qdestiny.lock               # 运行时锁文件（框架启动后自动创建）
│
├── core/                        # 核心模块
│   ├── __init__.py
│   ├── protocol.py              # 通信协议定义
│   ├── server.py                # 框架主进程
│   ├── client.py                # CLI 客户端
│   ├── config_loader.py         # YAML 配置加载与验证
│   ├── scheduler.py             # 定时任务调度引擎
│   ├── hotkey_manager.py        # 全局热键监听管理器
│   └── service_runner.py        # 服务生命周期管理
│
├── services/                    # 用户服务目录
│   └── <name>.py                # 服务文件（必须含 start() 函数）
│
├── configs/                     # 服务配置目录
│   └── <name>.yaml              # 对应的 YAML 配置文件
│
├── utils/
│   └── log.py                   # 日志模块说明
│
└── logs/                        # 日志输出目录
    └── qdestiny.log             # 框架运行日志（自动轮转）
```

---

## 3. 各模块详解

### 3.1 manage.py — CLI 入口

**职责**：解析命令行参数，决定启动模式。

**工作流程**：
1. 使用 `argparse` 定义 7 个子命令：`run / create / stop / status / list / remove / exit`
2. 初始化 `logging` 全局日志（控制台 + 文件轮转）
3. 如果命令是 `run` → 实例化 `FrameworkServer` 并调用 `start()`，进入主进程模式
4. 如果是其他命令 → 实例化 `FrameworkClient`，通过 TCP 发送命令到主进程，打印响应

**两种模式**：
- **主进程模式**（`run`）：长期运行，管理所有子系统
- **客户端模式**（其他命令）：发送一条命令，打印结果，立即退出

### 3.2 core/protocol.py — 通信协议

**职责**：定义主进程与客户端之间的 TCP 通信协议。

**协议格式**：JSON 文本 + 换行符 `\n` 作为消息分隔符

```
请求: {"cmd": "create", "args": {"service": "test"}}\n
响应: {"status": "ok", "data": {...}, "message": "..."}\n
```

**关键常量**：
- `DEFAULT_PORT = 19527`：默认监听端口
- `DEFAULT_HOST = '127.0.0.1'`：仅监听本地回环，不暴露到网络
- `LOCK_FILE_NAME = '.qdestiny.lock'`：锁文件名

**核心函数**：
- `encode_request(cmd, args)` → 编码请求为 bytes
- `encode_response(status, data, message)` → 编码响应为 bytes
- `recv_message(sock, timeout)` → 从 socket 读取一条完整消息并解码为 dict
- `send_message(sock, data)` → 发送 bytes 到 socket

### 3.3 core/server.py — 框架主进程 (FrameworkServer)

**职责**：核心枢纽，管理 TCP 服务器、调度器、热键管理器、服务运行器的生命周期。

**启动序列** (`start()`)：

```
1. 检查锁文件 → 防止多实例运行
   ├─ 锁文件存在 && PID 活跃 → 报错退出
   └─ 锁文件不存在或 PID 已死 → 继续

2. 创建 TCP Socket → bind(127.0.0.1:19527)
   ├─ 端口可用 → 绑定成功
   └─ 端口被占 → 自动分配随机端口

3. listen(5) + settimeout(1.0)

4. 写锁文件 (.qdestiny.lock)
   内容: {"pid": 12345, "port": 19527, "started_at": 1712345678.9}

5. 注册 atexit 回调清理锁文件

6. 启动子系统
   ├─ scheduler.start()        → 调度器线程启动
   └─ hotkey_mgr.start()       → 热键监听线程启动

7. 注册信号处理
   ├─ SIGINT (Ctrl+C)
   └─ SIGBREAK (Windows)

8. self._running.set()          → 设置运行标志

9. 启动 TCP 接收线程 (_tcp_loop)

10. 主线程进入等待循环
    while self._running.is_set():
        time.sleep(0.5)
```

**TCP 接收循环** (`_tcp_loop`)：
- 循环 `accept()` 新连接，每个连接在独立 daemon 线程中处理
- `settimeout(1.0)` 使 `accept()` 每秒超时一次，便于检查退出标志

**命令分发** (`_dispatch`)：

| cmd | 处理方法 | 调用链 |
|-----|---------|--------|
| `create` | `_cmd_create` → `service_runner.start_service()` | 加载配置 → 导入模块 → 注册到 scheduler 或 hotkey_mgr |
| `stop` | `_cmd_stop` → `service_runner.stop_service()` | 从子系统注销 → 更新状态 |
| `status` | `_cmd_status` → `service_runner.get_status()` | 收集框架 + 服务状态 |
| `list` | `_cmd_list` → `service_runner.list_available()` | 扫描 services/ 目录 |
| `remove` | `_cmd_remove` → `service_runner.remove_service()` | stop + 从注册表删除 |
| `exit` | `_cmd_exit` → 延迟 0.5s 后 `_running.clear()` | 先发响应，再触发关闭 |

**优雅关闭** (`_shutdown`)：
```
1. 关闭 TCP Server socket
2. service_runner.stop_all()   → 停止所有运行中的服务
3. hotkey_mgr.stop()           → 停止热键监听
4. scheduler.stop()            → 停止调度器
5. 删除 .qdestiny.lock
```

**锁文件安全**：
- 启动时检查：如果锁文件存在，读取 PID，通过 `psutil.pid_exists()` 判断进程是否活跃
- 进程不存在 → 旧锁文件是残留，删除后继续启动
- `atexit.register()` 兜底：即使异常退出也会尝试清理锁文件

### 3.4 core/client.py — CLI 客户端 (FrameworkClient)

**职责**：连接主进程，发送命令，格式化打印响应。

**流程**：
```
1. 读取 .qdestiny.lock → 获取端口号
   ├─ 文件不存在 → 提示 "框架未运行"
   └─ 文件存在 → 取出 port 字段

2. TCP 连接 127.0.0.1:port (超时 3 秒)
   ├─ 连接失败 → 提示 "无法连接到框架"
   └─ 连接成功 → 继续

3. 发送 JSON 请求

4. 接收 JSON 响应 (超时 10 秒)

5. format_response() 格式化输出
   ├─ create/stop/remove/exit → 单行 [OK]/[ERROR] + 消息
   ├─ status → 表格式展示框架状态 + 服务列表
   └─ list   → 列出可用服务及配置文件状态
```

### 3.5 core/config_loader.py — 配置加载器

**职责**：加载和验证服务 YAML 配置文件。

**核心函数**：

- `load_service_config(name)` — 读取 `configs/<name>.yaml`，返回 dict
- `validate_config(config)` — 验证配置合法性，返回 `(bool, error_msg)`
- `service_file_exists(name)` — 检查 `services/<name>.py` 是否存在
- `list_service_files()` — 扫描 services/ 目录列出所有 .py 文件，并检查 configs/ 目录下是否有对应 .yaml

**验证规则**：

| 字段 | 规则 |
|------|------|
| `name` | 必须存在，字符串类型 |
| `type` | 必须为 `"scheduled"` 或 `"hotkey"` |
| `schedule.delay` | >= 0 的数字 |
| `schedule.interval` | >= 0 的数字 |
| `schedule.repeat` | `"once"` / `"forever"` / 正整数 |
| `hotkey.keys` | 非空字符串 |

### 3.6 core/scheduler.py — 定时任务调度器 (TaskScheduler)

**职责**：高效调度所有 `scheduled` 类型服务的定时执行。

**数据结构**：
- `_heap`：最小堆，元素为 `(next_run_timestamp, counter, task_id)`
  - `counter` 单调递增，解决相同时间戳时的比较问题
- `_tasks`：字典，`task_id → task_info`

**task_info 结构**：
```python
{
    'id': 'test',
    'func': <callable>,         # 包装后的 start() 函数
    'interval': 10,             # 重复间隔（秒）
    'max_times': 3,             # 最大执行次数（None = 无限）
    'executed': 0,              # 已执行次数
    'executing': Event(),       # set=空闲, clear=执行中（用于防重入）
}
```

**调度主循环** (`_loop`)：

```
while running:
    1. 清除 wakeup 事件

    2. 加锁检查堆顶
       while 堆非空 && 堆顶时间 <= 当前时间:
           弹出 (timestamp, counter, task_id)
           ├─ task_id 不在 _tasks 中 → 跳过（已被删除）
           ├─ task.executing 未 set → 上次未完成，跳过，重新入堆
           └─ 正常 → 启动新线程执行 _run_task(task_id)

    3. 计算等待时间
       ├─ 堆非空 → wait = 堆顶时间 - 当前时间（最少 0.05s，最多 1s）
       └─ 堆为空 → wait = None（无限等待）

    4. wakeup.wait(timeout=wait)
       ├─ 新任务加入时 wakeup.set() 可提前唤醒
       └─ 超时后自动继续循环
```

**任务执行** (`_run_task`)：

```
1. 标记 executing.clear()   → 执行中
2. 调用 task['func']()      → 即用户服务的 start()
3. 标记 executing.set()     → 执行完毕
4. executed += 1
5. 检查执行次数
   ├─ 达到 max_times → 从 _tasks 删除，自动移除
   └─ 未达到 && interval > 0 → 计算 next_run，重新入堆
```

**防重入机制**：每个任务有一个 `threading.Event`，执行前 `clear()`，执行后 `set()`。调度时如果发现 Event 未 set，说明上次还没执行完，跳过并重新入堆。

### 3.7 core/hotkey_manager.py — 热键监听管理器 (HotkeyManager)

**职责**：全局捕获键盘按键，匹配注册的组合键并触发服务。

**实现思路**：

使用 `pynput.keyboard.Listener` 底层监听（而不是 `GlobalHotKeys`），因为 `GlobalHotKeys` 要求组合中必须包含修饰键，不支持纯字母组合如 `a+d`。

**数据结构**：
- `_registry`：`{service_name: {'keys': frozenset, 'callback': func, 'keys_str': str}}`
- `_key_to_services`：`{frozenset(keys): service_name}` — 反向查找
- `_pressed`：`set` — 当前按下的键集合
- `_executing`：`{service_name: bool}` — 防重入标志

**按键标准化** (`_normalize_key`)：
- `keyboard.KeyCode` → 取 `char.lower()`（如 `'a'`, `'d'`）
- `keyboard.Key` → 取 `name.lower()`（如 `'ctrl_l'`, `'shift'`）

**热键字符串解析** (`parse_hotkey_string`)：
```
"a+d"           → frozenset({'a', 'd'})
"ctrl+shift+z"  → frozenset({'ctrl_l', 'shift_l', 'z'})
```
内置映射表处理 `ctrl → ctrl_l`, `alt → alt_l` 等别名。

**触发流程**：
```
用户按下按键 → on_press(key)
  1. normalize_key(key) → 标准化键名
  2. 加入 _pressed 集合
  3. 遍历 _key_to_services:
     如果 registered_keys ⊆ _pressed:
       ├─ 该服务正在执行中 → 跳过（防重入）
       └─ 空闲 → 新线程执行 callback()

用户松开按键 → on_release(key)
  1. 从 _pressed 集合移除
```

**优雅降级**：如果 `pynput` 未安装，`PYNPUT_AVAILABLE = False`，`start()` 只打印警告，不会报错。

### 3.8 core/service_runner.py — 服务运行器 (ServiceRunner)

**职责**：服务生命周期的中枢，衔接配置系统与执行子系统（调度器/热键管理器）。

**ServiceInfo 数据结构**：
```python
{
    'name': 'test',
    'config': {...},           # 解析后的 YAML 配置
    'module': <module>,        # 已导入的 Python 模块
    'state': 'running',        # stopped / running / error
    'started_at': 1712345678,  # 启动时间戳
    'run_count': 3,            # start() 累计执行次数
    'error_msg': '',           # 最后一次错误信息
}
```

**start_service(name) 完整流程**：

```
1. 检查服务是否已在运行 → 是则返回错误

2. 检查 services/<name>.py 是否存在

3. 加载 configs/<name>.yaml → validate_config()
   ├─ 文件不存在 → 报错
   └─ 验证失败 → 报错

4. 动态导入模块
   importlib.import_module(f'services.{name}')
   ├─ 已导入过 → importlib.reload() 重新加载（支持修改后重启）
   └─ 首次导入 → import_module()

5. 检查 module.start 是否存在且可调用

6. 创建 wrapped_start() 包装函数
   → 调用 module.start()
   → 成功后 run_count += 1
   → 异常时记录 error_msg

7. 根据 type 注册到子系统
   ├─ scheduled → scheduler.add_task(name, wrapped_start, delay, interval, repeat)
   └─ hotkey   → hotkey_mgr.register(name, keys_str, wrapped_start)

8. 创建 ServiceInfo，标记 state='running'，存入 _services 字典
```

**stop_service(name)**：
```
1. 根据 type 从子系统注销
   ├─ scheduled → scheduler.remove_task(name)
   └─ hotkey   → hotkey_mgr.unregister(name)
2. 标记 state='stopped'
```

**remove_service(name)**：先 stop，再从 `_services` 字典删除。

---

## 4. 进程间通信机制

### 为什么选择 TCP Socket

| 方案 | Windows 兼容 | 实现复杂度 | 可靠性 |
|------|-------------|-----------|--------|
| Unix Domain Socket | 不支持 | 低 | 高 |
| 命名管道 | API 差异大 | 高 | 中 |
| 文件轮询 | 支持 | 中 | 低（延迟高） |
| **TCP Socket** | **完美支持** | **低** | **高** |

TCP Socket 是唯一在所有平台上表现一致且简单可靠的方案。绑定 `127.0.0.1` 确保不暴露到网络。

### 通信时序图

```
  Client                          Server
    │                               │
    │  读取 .qdestiny.lock          │
    │  获取端口号                    │
    │                               │
    │──── TCP connect ────────────►│
    │                               │ accept() → 新线程
    │──── JSON request + \n ──────►│
    │                               │ recv_message()
    │                               │ dispatch()
    │◄──── JSON response + \n ─────│
    │                               │ conn.close()
    │  close()                      │
    │  打印结果                      │
    │  进程退出                      │
```

---

## 5. 线程模型

框架运行时的线程分布：

```
主线程 (Main)
  └─ while running: sleep(0.5)     # 等待退出信号

TCP 服务线程 (TCPServer)
  └─ while running: accept()       # 接收客户端连接
      └─ 连接处理线程 (Conn-xxxxx)  # 每个连接一个线程，处理完即销毁

调度器线程 (Scheduler)
  └─ while running: 检查堆顶 → 执行
      └─ 任务线程 (Task-test)       # 每次触发创建，执行 start() 后销毁

热键监听线程 (pynput Listener)
  └─ 系统级键盘 hook 循环
      └─ 回调线程 (Hotkey-xxx)      # 触发时创建，执行 start() 后销毁
```

所有非主线程均为 `daemon=True`，主线程退出时自动终止。

---

## 6. 服务生命周期

```
           create               执行完成/手动stop
[未注册] ─────────► [running] ─────────────────► [stopped]
                       │                             │
                       │ start()异常                  │ create（重新启动）
                       ▼                             │
                    [error]                          │
                       │                             │
                       │ remove                      │ remove
                       ▼                             ▼
                   [已移除]                        [已移除]
```

- **create**：加载配置 → 导入模块 → 注册到子系统 → running
- **stop**：从子系统注销 → stopped（保留注册信息，status 可查）
- **remove**：stop + 从注册表删除（status 中不再出现）
- **自动完成**：scheduled 类型任务达到 repeat 次数后，调度器自动移除任务（但 ServiceInfo 保留，状态仍为 running）

---

## 7. 关键设计决策

### 7.1 start() 是一次性调用

`start()` 不是长驻循环。每次调度到期时，调度器在新线程中调用一次 `start()`，等它返回后根据 repeat 策略决定是否继续。这使得服务编写非常简单——用户只需关心"执行一次任务"的逻辑。

### 7.2 防重入

定时任务和热键触发都实现了防重入：
- **调度器**：通过 `threading.Event` 标记执行状态，上次未完成则跳过本次
- **热键管理器**：通过 `_executing` 字典标记，正在执行时忽略重复触发

### 7.3 模块热重载

每次 `create` 服务时，如果模块已导入过，会调用 `importlib.reload()` 重新加载。这意味着修改服务代码后，只需 `stop` → `create` 即可生效，无需重启框架。

### 7.4 锁文件机制

`.qdestiny.lock` 解决两个问题：
1. **进程发现**：客户端通过锁文件获取服务端端口号
2. **防多实例**：启动时检查锁文件中的 PID 是否活跃

使用 `psutil.pid_exists()` 跨平台检测进程是否存活，避免残留锁文件阻止启动。

---

## 8. 错误处理策略

| 场景 | 处理方式 |
|------|---------|
| 服务 start() 抛出异常 | 捕获，记录到 error_msg，不影响框架 |
| TCP 连接处理出错 | try/except 包裹，返回错误响应，关闭连接 |
| 配置文件格式错误 | validate_config 返回详细错误信息 |
| 模块导入失败 | 捕获 ImportError，返回友好提示 |
| 框架异常退出 | atexit 兜底清理锁文件 |
| pynput 未安装 | 优雅降级，热键功能不可用但框架正常运行 |
| 端口被占用 | 自动分配随机端口，写入锁文件 |
